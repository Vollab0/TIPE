git checkout : changer de branche
git branch : creer une nouvelle branche
git commit -a -m 'commentaire' : met tous les fichiers en attente d'envoi
git push : pousse les fichiers en attente sur le repository
git pull : mettre a jour                                                    <========= a faire a chaque ouverture de fichiers